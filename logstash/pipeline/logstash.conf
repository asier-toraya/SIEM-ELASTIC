input {
  beats {
    port => 5044
    ssl => false
  }
}

filter {
  # Keep it simple: we ship raw messages + "type" field from Filebeat.
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "filebeat-%{+YYYY.MM.dd}"
  }
}

